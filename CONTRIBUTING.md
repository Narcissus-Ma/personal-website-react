# 贡献指南

感谢你愿意改进本项目。提交前请先搜索已有 Issue 和 Pull Request，避免重复讨论。

## 反馈问题

- 可复现的缺陷请使用 Bug Report 模板，写明环境、复现步骤、实际结果与预期结果。
- 功能建议请说明使用场景和预期价值；保持每个 Issue 聚焦一个主题。
- 安全问题不要公开提交 Issue，请遵循 [安全策略](SECURITY.md)。

## 提交代码

1. Fork 仓库并从当前默认分支创建主题分支。
2. 保持改动范围小且聚焦；新建 React 组件请使用函数组件与 Hooks，并为 TypeScript 公共结构定义明确类型。
3. 如改动影响行为、配置或使用方式，同时更新相应文档。
4. 提交前运行：

   ```bash
   pnpm lint
   pnpm build
   ```

5. 提交 Pull Request，说明动机、改动内容、验证方式和已知限制。

请不要提交密钥、令牌、个人域名配置、Cloudflare namespace ID 或 `.env.local` 内容。涉及 Cloudflare Worker 写入接口的变更，应明确评估访问控制与 CORS 影响。

## 沟通方式

讨论应友善、具体且可验证。维护者可能会根据项目方向、维护成本和安全影响决定是否采纳贡献；这不构成对响应时间或合并结果的承诺。
