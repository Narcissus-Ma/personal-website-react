# 安全策略

## 当前安全边界

本项目目前**不应被视为可安全暴露到不受信任公网的管理系统**。Cloudflare Worker 存在以下已知限制：

- `POST /api/save` 没有鉴权，能够访问 Worker 的客户端可尝试写入数据。
- `pnpm run cf:set-password` 仅设置管理页面密码，**不能**保护 `/api/save`。
- Worker 默认返回 `Access-Control-Allow-Origin: *`，即 CORS 允许任意来源。

因此，在自行实现并验证服务端访问控制、密码安全存储及受限 CORS 前，请只在可信私有环境使用。请不要将本项目的现状表述为安全、生产级或适合公开管理入口的方案。

## 支持范围

安全修复是否提供、修复范围与发布时间由维护者根据实际情况决定；本项目不承诺支持期限或响应时限。使用者应自行评估部署风险并采取必要的防护措施。

## 私密报告漏洞

请不要在公开 Issue、Pull Request 或讨论区披露漏洞利用步骤、凭据、完整复现代码或受影响实例信息。

优先使用仓库 Security 页面提供的 **Report a vulnerability**（GitHub Security Advisories）私密报告入口；若该入口不可用，请通过维护者在仓库资料页明确公布的私密联系渠道报告。报告可包含：受影响版本或提交、影响说明、最小复现步骤、潜在缓解措施，以及安全的回联方式。

收到报告后，维护者会在能力范围内评估；在问题得到缓解或公开协调完成前，请避免公开披露细节。
